If you want to use these materials using URP, follow these steps.
1) Open the shader by double clicking
2) Open the graph inspector, located at the top
3) Under "Target settings" you will see it's set to HDRP. Click the + button and add URP as a target
4) Remove the HDRP target
----------
Otherwise, you can look at the graph and copy it. It's really easy